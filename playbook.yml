-
  name: "Update packages"
  hosts: all
  become: true
  tasks:
    - name: apt-get update
      apt:
        update_cache: yes
      register: apt_update

    - name: Debug output
      debug:
        var: apt_update.stdout_lines
       
